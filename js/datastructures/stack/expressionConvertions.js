const { Stack } = require('./stack');

//1. Infix to Postfix
/*
    There are 3 types of expressions based on the place where the operand is present
    Infix - where the operator is in between the operands
    Postfix - where the operator is after the operands
    Prefix - where the operator is before the operands
*/
function infixToPostfixConversion(inputExp){
    let result = '';

    return result;
}

module.exports = {
    infixToPostfixConversion
}