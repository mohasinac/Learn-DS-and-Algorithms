/*
    Shortest paths ,

    SSSP single source shortest path
        1. the shortest path from a vertex x to every other vertex in the graph G
    APSP ; All pairs shortest path 
        1. from every vertex to every other vertex

    weights can be +ve or  -ve
    +v : roads
    -ve : bank account graph where A has a loan from B then a is -ve but B is +ve
*/