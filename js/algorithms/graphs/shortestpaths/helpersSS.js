/*
    for each vertex 
    set the distance as Infinity 
    set the parent/predecessor as null

    set the distance for source node as 0
    similar to prims
*/
function initializeSingleSource(graph,source){

}

/*
    if v.d > u.d + edge between u,v
    then v.d would be u.d + edge from u,v
    v. parent is u 
*/
function Relax(u,v,w){

}